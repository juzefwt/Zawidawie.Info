<div class="alternative">
    <h2>Firmy i usługi</h2>
    <form action="{{ path('catalog_index') }}" method="post" {{ form_enctype(searchForm) }}>
    {{ form_label(searchForm.query) }}
    {{ form_widget(searchForm.query) }}
    {{ form_rest(searchForm) }}
    <input type="submit" class="button medium green" value="Szukaj" />
    </form>
    <br />
    <h2>Często szukane</h2>
    <ul>
    {% for keyword in keywords %}
      <li><a href="{{ path('catalog_list', {'query': keyword}) }}">{{ keyword }}</a></li>
    {% endfor %}
    </ul>
    <br class="clear"/>
    <br class="clear"/>
    <h3 style="float:right">Prowadzisz firmę w okolicy?&nbsp;<a href="{{ path('catalog_new') }}" class="plain button big orange" style="color:#fff;text-decoration:none">Dodaj firmę</a></h3>
    <br class="clear"/>
</div>
<br class="clear"/>